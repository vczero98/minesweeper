var b = new Block(4, 5);
printParagraph("Area: " + b.getArea());
//document.body.style.cursor = "url('axe.png'), auto";
board = new Board(10, 10, 10);
function printParagraph(text) {
	document.write("<p>" + text + "</p>");
}
